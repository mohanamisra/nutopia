import React from 'react';

const Pipe = () => {
    const pipeStyle = {
        container: {
            width: "100px",
            height: "100%",
            backgroundColor: "white",
        }
    }

    return (
        <div className="pipe-container" style={pipeStyle.container}>
            
        </div>
    );
};

export default Pipe;